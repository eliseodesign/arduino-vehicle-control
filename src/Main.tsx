import { StyleSheet, View, Text } from 'react-native'

const Main = () => {
  return (
    <View>
      <Text>Arduino Vehicle Control v1.0.0</Text>
    </View>
  )
}
export default Main

const styles = StyleSheet.create({
  main:{
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  }
})

